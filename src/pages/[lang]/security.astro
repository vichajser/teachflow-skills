---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Icon from '@/components/Icon.astro';
import { SITE } from '@/config/site';
import { SECURITY_MATRIX, VERIFIABLE_FACTS } from '@/data/security-matrix';
import { useTranslations } from '@/i18n/t';
import type { Locale } from '@/i18n/config';

export { localeStaticPaths as getStaticPaths } from '@/i18n/paths';

const lang = Astro.params.lang as Locale;
const t = useTranslations(lang);
---

<BaseLayout
  lang={lang}
  title={`${t('security.title')} — ${SITE.productName}`}
  description={t('security.lead')}
  path="/security"
>
  <h1 class="pt-16 text-4xl font-semibold text-text-hi">{t('security.title')}</h1>
  <p class="mt-3 max-w-2xl text-lg text-text-body">{t('security.lead')}</p>

  <section class="mt-12">
    <h2 class="mb-4 text-xl font-semibold text-text-hi">{t('security.facts')}</h2>
    {/*
      图标与文字并排（`flex` + `gap`），不是浮在文字上方：这两条是整页
      最该被读完的句子，图标只负责让它们一眼可辨，不该把文字往下挤一行。
      `items-start` + 图标自带的 `shrink-0`：文案换行时图标钉在第一行行首，
      不随文字块垂直居中漂移。
    */}
    <ul class="grid gap-4 md:grid-cols-2">
      {VERIFIABLE_FACTS.map((fact) => (
        <li class="flex items-start gap-3 rounded-xl border border-accent-glow bg-surface p-5 text-text-body">
          <Icon name={fact.icon} size={22} class="mt-0.5 text-accent-glow" />
          <span>{fact.text[lang]}</span>
        </li>
      ))}
    </ul>
  </section>

  <section class="mt-12 pb-16">
    {/*
      tabindex="0" + role="region" + aria-label：这个 div 实测在 390px 下真的
      在横滚（/en/security/ 溢出 30px、/ko/security/ 8px），而它内部**一个可
      聚焦元素都没有**——鼠标用户能拖，键盘用户永远看不到被截掉的列
      （axe scrollable-region-focusable，WCAG 2.1.1）。tabindex 让它进入 Tab
      序列并可用方向键滚动；role+label 让读屏器报出这是什么区域，否则 Tab
      停在一个无名 group 上。label 走 i18n，不硬编码。
      焦点圈由 `focus-visible:` 变体给（键盘聚焦才描边，鼠标点击不闪框）。
    */}
    <div
      tabindex="0"
      role="region"
      aria-label={t('a11y.scrollableTable')}
      class="overflow-x-auto rounded-xl border border-border focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent"
    >
      <table class="w-full border-collapse text-left text-sm">
        <thead class="bg-raised text-text-hi">
          <tr>
            <th scope="col" class="px-4 py-3 font-semibold">{t('security.col.scan')}</th>
            <th scope="col" class="px-4 py-3 font-semibold">{t('security.col.practice')}</th>
            <th scope="col" class="px-4 py-3 font-semibold">{t('security.col.verify')}</th>
          </tr>
        </thead>
        <tbody>
          {SECURITY_MATRIX.map((row) => (
            <tr class="border-t border-border align-top">
              <th scope="row" class="px-4 py-3 font-mono text-xs font-normal text-text-hi">
                {row.scan}
              </th>
              <td class="px-4 py-3 text-text-body">{row.practice[lang]}</td>
              <td class="px-4 py-3 text-text-mute">{row.verify[lang]}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </section>
</BaseLayout>
